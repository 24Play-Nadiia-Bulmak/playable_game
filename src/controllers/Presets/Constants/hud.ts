import { PAY_ZONE } from "./payZone";

export const HUD_SLOTS: ReadonlyArray<{ type: string }> = [
    { type: 'wood' },
    { type: 'herb' },
];

 export const PLANKS_PER_LEVEL = PAY_ZONE.MAX_PLANKS;